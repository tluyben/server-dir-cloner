You are an expert all-round developer who knows everything about Next.js, TypeScript, Drizzle, SQLite, HTML5, and React. Study the current project and implement the following feature:

Task: here we are building a server that can be installed in linux and that clone directory changes from one server to another

Description:

<p>write a <a target="_blank" rel="noopener noreferrer nofollow" class="text-blue-500 underline hover:text-blue-600 cursor-pointer" href="http://prd.md">prd.md</a> for this project first what you have to do. So ; we install this project on 2 servers, call them serverA + serverB</p><p></p><p>when a user (with curl, some api etc) points to serverA; </p><p></p><p>POST /add directory {dir: /home/xxx/bla/bla, targets: ['serverB']} <br><br>then  serverA posts automatically to serverB: {dir: /home/xxx/bla/bla, targets: ['serverA']} (so that will indicate it's bi-directional relation </p><p></p><p>it will then create on either or both server  /home/xxx/bla/bla directory (of couse not if it already exists; it will recursively make sure they are the same where serverA is the LEADER so the first sync will mirror A-&gt;B </p><p></p><p>now when ANYTHING changes on either server IN that directory or its subdirs, it will mirror that to the other side A&lt;&gt;B -&gt; updates, deletes, adds etc </p><p></p><p>it wil STORE the server topology in sqlite and will also log all actions in sqlite </p><p></p><p>first create the <a target="_blank" rel="noopener noreferrer nofollow" class="text-blue-500 underline hover:text-blue-600 cursor-pointer" href="http://prd.md">prd.md</a> plan to make all this happen</p>

Type: Program
Priority: medium

IMPORTANT:

- Study the project's CLAUDE.md and README.md for conventions and standards
- Follow all existing patterns and best practices in the codebase
- Ensure TypeScript strict compliance
- Write comprehensive tests for your implementation
- Update documentation as needed
- If there are any images or files (images, txt, md, mermaid files ONLY) that the user MUST see, put them at the END of your conclusions as paths in this format:
  {description}: ./somefile.png
  {description}: /tmp/analysis.md
  Example: "Proposed UI design: ./pagedesign.png"
